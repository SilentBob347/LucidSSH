node_modules/
out/
dist/
release/
*.log
.DS_Store
Thumbs.db
.vite/
coverage/
*.tsbuildinfo

# Локальные бэкапы дизайна (тяжёлые zip); источник истины по UI — docs/
Design backup/

# Локальный issue-tracker (docs/agent/issue-tracker.md) — вне репозитория намеренно
.scratch/

# Внутренняя документация (ТЗ, roadmap, ADR) — не публикуется, вычищена из истории
private/
